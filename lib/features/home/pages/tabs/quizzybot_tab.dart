import 'package:flutter/material.dart';

class QuizzyBotTab extends StatelessWidget {
  const QuizzyBotTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('QuizzyBot', style: TextStyle(color: Colors.white)));
  }
}
